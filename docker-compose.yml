services:
  leaderboard:
    build: .
    image: binance-leaderboard-workflow:latest
    network_mode: host
    environment:
      DATA_ROOT: /data
      WEB_HOST: 127.0.0.1
      WEB_PORT: "48234"
      ACCESS_KEY: "002127"
    volumes:
      - ./data:/data
      - ./web:/app/web:ro
      - ./app.py:/app/app.py
      - ./auto_leaderboard.py:/app/auto_leaderboard.py
    restart: unless-stopped
